syntax = "proto3";

package io.mark.grpc.grpcChat;

service ChatService {
    rpc chat(ChatMessage) returns (ResponseMessage) {}
}

message ChatMessage {
    string from = 1;
    string message = 2;
}

message ResponseMessage {
  string isSuccess = 1;
  string message = 2;
}
